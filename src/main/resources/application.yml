quarkus:
    kubernetes:
        deployment-target: knative
    readiness-probe:
      initial-delay: 20s
      period: 45s
    swagger-ui:
      always-include: true
    container-image:
      builder: docker
      group: org.rr.terminal
      name: punctual-phoenix-kn
